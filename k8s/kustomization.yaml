apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: infra-ops
resources:
  # 00-namespace.yaml KHÔNG nằm trong danh sách này — namespace `infra-ops` đã
  # được tạo sẵn qua KubeSphere Console (Project infra-ops).
  - 01-configmap.yaml
  - 02-secret.yaml   # bạn tự tạo từ 02-secret.example.yaml — không commit bản thật
  - 03-postgres.yaml
  - 04-app.yaml
